<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ReadBook.Views.BookDetailPage"
             Title="{Binding Title}">
	<ContentPage.ToolbarItems>
  	  	<ToolbarItem Text="{Binding ReadText}" Clicked="Read_Clicked" />
    </ContentPage.ToolbarItems>
	<StackLayout Spacing="20" Padding="15">
		<Label Text="{Binding Book.Title}" FontSize="Medium"/>
        <Label Text="{Binding Book.Genre}" FontSize="Medium"/>
		<Label Text="Páginas" FontSize="Medium"/>
		<Label Text="{Binding Book.Pages}" FontSize="Medium"/>
		<Label Text="{Binding Book.Description}" FontSize="Small"/>
    </StackLayout>
</ContentPage>